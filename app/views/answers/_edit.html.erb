<%= render 'errors' %>

<div>
  <div class="am-u-sm-3"></div>
  <div class="am-u-sm-9 am-u-sm-offset-3">
    <span class="am-monospace" style="font-weight: bold">修改回答</span>
    <hr style="margin-top: 0px;">
    <%= form_for [@question, @answer] do |f| %>
        <%= f.cktext_area :content%>
        <div style="margin-top: 10px;">
          <%= f.submit "提交", class:"am-btn am-btn-primary", style:"float:right"%>
        </div>
    <% end %>
    <!--
    <%= button_to '返回', @question, action:"show", method: :get , :class => 'am-btn', :style => 'float:right; margin-right:5px'%>
    -->
  </div>
</div>
