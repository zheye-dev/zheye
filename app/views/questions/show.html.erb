<% content_for :head do %>
    <title><%= @question.title %>-者也</title>
<% end %>

<%= render partial: 'question_box', locals: {detail: true} %>
<%= content_tag "div", "Loading...", id: "answer_box", data: {question_id: @question.id} %>
<%= content_tag "div", "Loading...", id: "new_answer_box", data: {question_id: @question.id, user_login: current_user.login} %>


