<%= debug(@user) %>

<% if can? :update, @user %>
  <%= link_to 'Edit', edit_user_path(@user) %>
<% end %>

<% if can? :destroy, @user %>
  <%= link_to 'Destroy', user_path(@user), method: :delete, data: {confirm: 'Really?'} %>
<% end %>